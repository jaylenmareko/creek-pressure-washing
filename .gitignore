.vercel

# Secrets / credentials (never commit)
.env
.env.*
*.pem
*.key
*.crt
credentials*.md
credentials*.json
secrets*.md
secrets*.json

# Scraped lead data — contains PII (emails, owner names)
outreach/data/
